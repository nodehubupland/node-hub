name: Keep current user dashboard loader

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  patch-index:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v4
      - name: Ensure current user dashboard loader
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p = Path('index.html')
          s = p.read_text(encoding='utf-8')
          s, count = re.subn(r'<script src="user-dashboard\.js\?v=[^"]+"></script>', '<script src="user-dashboard.js?v=20260828-v9"></script>', s, count=1)
          if count == 0:
              raise SystemExit('user-dashboard.js script marker not found')
          p.write_text(s, encoding='utf-8')
          PY
      - name: Commit loader change
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Use refreshed user dashboard loader'
          git push
